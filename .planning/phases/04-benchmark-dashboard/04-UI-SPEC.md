---
phase: 4
slug: benchmark-dashboard
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-21
---

# Phase 04 — UI Design Contract

> Visual and interaction contract for Phase 04-04: MapPage Network Risk Polish.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> Scope: four targeted fixes to `frontend/src/pages/MapPage.tsx` — toggle layout (D-01),
> animate-ping halo (D-02), marker-click scroll+highlight (D-03), loading spinner (D-04).
> Plans 04-01 through 04-03 are fully implemented and committed.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — manual Tailwind utility classes |
| Preset | not applicable |
| Component library | none (custom Tailwind components) |
| Icon library | lucide-react ^1.7.0 (Route, Globe, X, ChevronDown, MapPin, Search, Package, Tag already imported) |
| Font | system-ui stack: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif |

Source: `frontend/src/index.css` (body font-family), `frontend/package.json` (lucide-react).
No `components.json` found; no shadcn initialization applicable.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gaps (`gap-1`), tight inline padding |
| sm | 8px | Button padding inner (`px-2 py-1`), compact elements |
| md | 16px | Panel section padding, control group gaps |
| lg | 24px | Side panel padding (`p-4` = 16px to `p-6` = 24px range) |
| xl | 32px | Not used in this phase |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Toggle top offset: `top-4` (1rem = 16px) — permanent position, locked by D-01
- Route Stops top offset: `top-16` (4rem = 64px) — moved below toggle by D-01; replaces previously noted `top-14` (56px is not a standard-scale value)
- Spinner size: 12px × 12px (`w-3 h-3`) — pre-existing technical debt matching `routeLoading` spinner in MapPage.tsx line 375; not introduced by 04-04, not a new violation
- Tab button inline gap: `gap-2` (8px) — between spinner and "Loading…" text inside Network Risk tab

Source: CONTEXT.md D-01, RESEARCH.md Pattern 2, codebase verified.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 |
| Label / UI control | 12px (`text-xs`) | 600 (semibold — `font-semibold`) | 1.4 |
| Micro / badge | 10px (`text-[10px]`) | 600 (semibold) | 1.2 |
| Heading (panel title) | 18px (`text-3xl` in current code — override to `text-lg`) | 600 (semibold) | 1.2 |

Notes:
- The current side panel heading uses `text-3xl font-semibold` (line 793) — this is a known visual outlier. Phase 04-04 does NOT touch this value (it is pre-existing; restyling is deferred).
- All new text introduced in 04-04 (spinner label "Loading…", highlight ring) uses `text-xs font-semibold` — consistent with toggle button siblings.
- Two weights only: regular (400) for body/captions, semibold (600) for labels, buttons, headings.

Source: `frontend/src/pages/MapPage.tsx` grep, `frontend/src/components/NavBar.tsx` grep.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0f172a` (slate-900) | Map page background, panel backgrounds, overlay fills |
| Secondary (30%) | `#1e293b` (slate-800) | Component row backgrounds (`bg-slate-800/40`), control pill backgrounds, tooltip backgrounds |
| Accent (10%) | `#6366f1` (indigo-600) | Active tab state only: `bg-indigo-600` on the Routes/Network Risk toggle |
| Destructive | `#ef4444` (red-500) | Single-source risk: halo fill (`bg-red-500/60`), panel border accent (`border-red-500`), row highlight ring (`ring-red-400`) |

Accent (`#6366f1`) reserved for:
- Active state of the Routes/Network Risk toggle pill (`bg-indigo-600 text-white` when `mapView === 'network-risk'` or `=== 'routes'`)
- Nothing else — do not use indigo on the spinner, highlight rings, or markers

Destructive (`#ef4444`) reserved for:
- `animate-ping` halo ring on single-source distributor markers: `bg-red-500/60 absolute inset-0 rounded-full animate-ping`
- Left border accent on side panel component rows: `border-l-2 border-red-500`
- Selected row highlight outline (D-03): `ring-1 ring-red-400`

Secondary semantic colors already in codebase (not new, not overridden in 04-04):
- Blue (`#3b82f6` / blue-500): existing spinner borders (`border-blue-500`), loading text (`text-blue-400`) — do NOT use for Network Risk elements
- Green (`#22c55e` / green-600): existing hub marker tooltip — untouched in 04-04

Source: `frontend/tailwind.config.js` (custom color tokens), `frontend/src/index.css` (`background-color: #0f172a`), CONTEXT.md specifics section, RESEARCH.md Pattern 1.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Network Risk tab — loading state | "Loading…" (with trailing ellipsis; inline after CSS spinner) |
| Network Risk tab — loaded state | "Network Risk" (unchanged) |
| Routes tab label | "Routes" (unchanged) |
| Side panel — empty state heading | "No single-source components detected" |
| Side panel — empty state body | (no body copy — heading alone is sufficient for empty state) |
| Side panel — loading state | (panel renders empty state copy until `singleSourceComponents` resolves — no separate loading copy needed per D-04) |
| Side panel — panel title | "Single-source components" (unchanged from current line 793) |
| Side panel — subtitle | "Components with a single known distributor represent critical supply chain risk." (current line 794 — unchanged) |
| Route Stops button label | "Route Stops" (unchanged — button position moves, label does not) |
| Marker tooltip (hover) | "{distributor_name}" — unchanged from current implementation |
| Error state — graphAPI.metrics() fails | On `.catch()`: set `graphMetricsError: boolean` state to `true`; render inline message in the Network Risk tab panel header: "Risk data unavailable — reload to retry" (text-xs, text-slate-400, no icon). `graphMetricsLoading` returns to `false`. Tab label returns to "Network Risk". |

Destructive actions in this phase: none. No delete, remove, or irreversible actions are introduced in 04-04.

Source: CONTEXT.md D-04, RESEARCH.md Pattern 4, codebase lines 793-794, 809.

---

## Interaction Contract

### D-01: Toggle and Route Stops Layout

- Toggle pill (`Routes` | `Network Risk`) — permanently at `top-4 right-14 z-10`
- Route Stops button — moves from `top-4 right-14` to `top-16 right-14 z-10`
- Both use `absolute` positioning on the map container
- Route Stops remains conditionally rendered: only when `selectedRoute && mapView === 'routes'`
- No animation on position change — class swap is instant

### D-02: Single-Source Halo Ring Animation

- Element: `<div className="absolute inset-0 rounded-full bg-red-500/60 animate-ping" aria-hidden="true" />`
- Parent container must have `position: relative` (already satisfied by existing marker wrapper)
- Halo is a sibling of the marker dot — both inside the same relative container
- `animate-ping` is unconditional (no `motion-safe:` prefix) — animation carries semantic meaning (risk alert)
- No `ring-*` classes on the halo element — fill-based approach only

### D-03: Marker Click → Side Panel Scroll and Highlight

- New state: `selectedDistributorId: number | null` — initialized to `null`
- Marker `onClick` in Network Risk branch: `setShowNetworkRiskPanel(true); setSelectedDistributorId(dist.id)`
- Panel close button `onClick`: `setShowNetworkRiskPanel(false); setSelectedDistributorId(null)`
- Row highlight class: `ring-1 ring-red-400` — applied when `selectedDistributorId === comp.distributor_id`
- Row base class preserves existing: `w-full text-left bg-slate-800/40 hover:bg-slate-800/70 rounded-lg px-3 py-2 border-l-2 border-red-500 transition-colors`
- Scroll: `useEffect` keyed on `[selectedDistributorId]` calls `el.scrollIntoView({ behavior: 'smooth', block: 'nearest' })`
- Ref map: `useRef<Map<number, HTMLButtonElement>>(new Map())` — one ref per distributor_id (last rendered row wins on duplicates — acceptable v1 tradeoff per RESEARCH.md Pitfall 2)
- Highlight clears to `null` when panel closes or a different marker is clicked

### D-04: Loading Spinner on Network Risk Tab

- New state: `graphMetricsLoading: boolean` — initialized to `false`
- New state: `graphMetricsError: boolean` — initialized to `false`
- Set `graphMetricsLoading = true` and `graphMetricsError = false` before `graphAPI.metrics()` fetch
- On `.then()`: set `graphMetricsLoading = false`
- On `.catch()`: set `graphMetricsLoading = false`, set `graphMetricsError = true`
- Cancelled-flag pattern required: `let cancelled = false; return () => { cancelled = true; }` — prevents setState after unmount (existing pattern from MapPage.tsx lines 196-229)
- Button width stability: apply `min-w-[96px]` to the Network Risk button to prevent toggle pill resizing when label swaps to spinner+text
- Spinner element: `<div className="w-3 h-3 rounded-full border border-current border-t-transparent animate-spin" />`
- `border-current` inherits text color (white when active `indigo-600`, `text-slate-400` otherwise) — no hard-coded color
- Label during loading: spinner + "Loading…" (`flex items-center gap-2`)
- Label when loaded: "Network Risk" (text only, no flex wrapper change needed)
- Error message placement: inline in the Network Risk panel header, below the panel title, when `graphMetricsError === true`; copy: "Risk data unavailable — reload to retry" (`text-xs text-slate-400`)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No third-party registries. No shadcn components. All UI is implemented via inline Tailwind utility classes on standard HTML/React elements.

---

## Component Inventory for This Phase

All changes are within `frontend/src/pages/MapPage.tsx`. No new files. No new components.

| Change | Element | New Classes / Props |
|--------|---------|---------------------|
| D-01 toggle | `<div>` toggle wrapper | Stays `top-4 right-14` — no change |
| D-01 Route Stops | `<button>` Route Stops | `top-4` → `top-16` |
| D-02 halo | `<div>` halo inside marker | Remove `ring-2 ring-red-500 ring-offset-1 motion-safe:animate-pulse`; add `bg-red-500/60 animate-ping` |
| D-03 state | `useState` | Add `selectedDistributorId` (`number \| null`) |
| D-03 refs | `useRef` | Add `componentRowRefs` (`Map<number, HTMLButtonElement>`) |
| D-03 row class | `<button>` component row | Add conditional `ring-1 ring-red-400` |
| D-03 effect | `useEffect` | Add scroll effect keyed on `selectedDistributorId` |
| D-04 state | `useState` | Add `graphMetricsLoading` (`boolean`), add `graphMetricsError` (`boolean`) |
| D-04 tab button | `<button>` Network Risk tab | Add `min-w-[96px]`; conditional spinner vs text label; `gap-2` in flex row |
| D-04 error | `<p>` inline panel header | Render "Risk data unavailable — reload to retry" when `graphMetricsError === true` |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
