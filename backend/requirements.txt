fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.13.0
psycopg[binary]==3.3.3
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1
python-dotenv==1.0.0
httpx==0.25.1
pytest==7.4.3
pytest-asyncio==0.23.0

# ML & optimization
prophet==1.3.0
# NOTE: pandas / numpy / scikit-learn are pinned to the EXACT versions that
# pickled backend/data/ml_models/*.joblib (see the `sklearn_version` field in
# metrics.joblib["provenance"]). joblib/pickle of sklearn estimators is NOT
# portable across versions: with the previous pins (numpy 1.26.2 /
# scikit-learn 1.3.2) the deployed instance failed to unpickle lead_time.joblib
# with `ModuleNotFoundError: No module named '_loss'` -- sklearn >=1.4 stores
# GradientBoostingRegressor's Cython loss under a bare `_loss` module name that
# 1.3.2 never puts in sys.modules -- and numpy 2 pickles the RNG BitGenerator in
# a form numpy 1.26 rejects. The startup handler swallowed both, so the API
# silently served model_source="none". If you retrain, re-pin these to match.
pandas==2.3.3
numpy==2.4.4
scikit-learn==1.8.0
joblib==1.5.3
ortools==9.7.2996
networkx==3.6.1

# Scheduling & data ingestion
apscheduler==3.11.2
openpyxl==3.1.5
xlrd==2.0.1              # legacy OLE2 .xls parsing (NY Fed GSCPI download)
datasets==5.0.1          # HuggingFace loader — REQUIRED by seeds/seed_db.py:164.
                         # It is a hard import at the top of seed(), not a
                         # fallback: without it `manage.py seed` dies with
                         # ModuleNotFoundError on a clean checkout. It was only
                         # listed (unpinned) in requirements_minimal.txt, which
                         # render.yaml never installs — render.yaml line 7 runs
                         # `pip install -r requirements.txt`, this file.

# MLOps — experiment tracking + model registry (P5)
mlflow==3.14.0

# OpenTelemetry (Phase 6 - performance monitoring)
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-exporter-jaeger==1.21.0
opentelemetry-instrumentation-fastapi==0.42b0
opentelemetry-instrumentation-sqlalchemy==0.42b0
opentelemetry-instrumentation-requests==0.42b0
