# Supply Chain Optimization Project

FastAPI backend, React frontend, PostgreSQL, OR-Tools, Prophet, Docker.

See `docs/` for project roadmap and state.

**Read `LEARNINGS.md` before you start.** It is the record of mistakes the autonomous loop has
already made in this repo. See `docs/AUTONOMOUS-LOOP.md` for how that loop works.
